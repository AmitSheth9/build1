//create a function that takes a base and exponent and raises the base to the exponent power

const baseExponent = (base, exponent) => {
    return Math.pow(base, exponent);
}

module.exports = baseExponent;